name: transaction_client
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get: ^4.6.5
  dio: ^4.0.0
  cookie_jar: ^3.0.1
  dio_cookie_manager: ^2.0.0
  shared_preferences: ^2.0.5
  call: ^0.3.0
  bot_toast: 4.0.0
  window_manager: ^0.2.1
  native_context_menu: ^0.2.1+4
  syncfusion_flutter_datepicker: ^20.1.48
  syncfusion_localizations: ^20.1.50
  json_dynamic_widget: 4.1.1+1
  path_provider: ^2.0.9
  tray_manager: ^0.1.7 #系统托盘
  flutter_swiper_plus: ^2.0.4
  docking: ^1.4.1+1
  multi_split_view: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: AliIcons
      fonts:
        - asset: assets/icons/iconfont.ttf
